Using domain knowledge to extact features from raw data. These features are used to improve performance of ML model.

```mermaid
flowchart TD
    Dataset[("Dataset")] --> Retrieve["Retrieve"]
    
    subgraph DataPrep ["Data Prep"]
        direction TB
        PD["Process Data"]
        FE["Feature Extract & Engg."]
        FS["Feature Scaling"]
        FSEL["Feature Selection"]
    end
    
    Retrieve --> DataPrep
    
    ML["ML Algo"] --> Modelling["Modelling"]
    DataPrep --> Modelling
    
    Modelling --> Eval["Eval"]
    Eval --> Deploy["Deploy"]
    
    Eval -- "Keep doing until model performs well" --> DataPrep

    %% Styling for Obsidian dark/light mode compatibility
    style Dataset fill:#f9f,stroke:#333,stroke-width:2px
    style Deploy fill:#bbf,stroke:#333,stroke-width:2px
```


### Feature transformation

- Missing values imputation (handling missing values)
- Handling categorical values
- Outlier detection
- Feature scaling

### Feature construction

- Create new features
- Splitting columns

### Feature selection

Select specific features

### Feature extraction

Extract new features programmatically




